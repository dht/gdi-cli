import { id } from '../$CMP';

export const params: IWidgetParams = {
    id,
    schema: {
        strings: {
            ctaButtonText: { fieldType: 'text', order: 0 },
        },
        colors: {},
        extra: {},
    },
};
