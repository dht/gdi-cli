import { base } from '@gdi/stores';

export const actions = base.actions;

export const selectors = {
    raw: base.selectors,
};

export const reducers = base.reducers;
export const initialState = base.initialState;
